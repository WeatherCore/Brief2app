# 密钥配置(只入库 .env.example 模板,真 .env 绝不入库)
.env

# Python 缓存
__pycache__/
*.pyc

# 日志(统一在 backend/logs/)
backend/logs/*.log

# sqlite 运行期临时文件
*.db-shm
*.db-wal

# 虚拟环境
.venv*/

# 工程师真写代码的工作区(每次构建一个 workspace/<run_id>/,体积会涨,不入库)
backend/workspace/

# 注:backend/data/runs.db 保留(演示历史)
